services:
  backend:
    build: ./backend
    restart: always
    working_dir: /app
    env_file:
      - .env
    environment:
      DEPLOYMENT_ROOT: /deployment
      SPLUNKAPPS_PATH: /deployment/apps
      # - GIT_AUTHOR_NAME=Splunk Ops
      # - GIT_AUTHOR_EMAIL=ops@example.com
    volumes:
      - ${DEPLOYMENT_ROOT}:/deployment
      - backend-data:/app/data
      # To enable Ansible SSH access, uncomment:
      # - ~/.ssh:/root/.ssh:ro

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend-data:
